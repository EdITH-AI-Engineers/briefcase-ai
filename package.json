{
  "name": "briefcase-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Briefcase AI Bun workspace monorepo.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "cd packages/synth-data-gen && bun run generate",
    "analyze": "cd packages/genai-analysis && bun run analyze",
    "evaluate": "cd packages/genai-analysis && bun run evaluate",
    "pipeline": "bun run generate && bun run analyze && bun run evaluate",
    "api": "cd packages/api && bun run dev",
    "ui": "cd packages/ui && bun run dev",
    "dev": "sh -c 'trap \"kill 0\" INT TERM EXIT; bun run api & bun run ui & wait'",
    "test": "cd packages/api && bun run test",
    "typecheck": "cd packages/shared && bun run typecheck && cd ../synth-data-gen && bun run typecheck && cd ../genai-analysis && bun run typecheck && cd ../api && bun run typecheck && cd ../ui && bun run typecheck",
    "build": "cd packages/ui && bun run build"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "lefthook": "^2.1.6"
  }
}
